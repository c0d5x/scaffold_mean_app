machine:
  node:
    version: 6.7.0
dependencies:
  pre:
    - ls -l `which npm`
    - npm prune
    - npm install -g npm@latest
    - ls -l `which npm`
    - npm install -g gulp-cli
    - gulp build
    - npm install --no-optional
test:
  override:
    - npm test
