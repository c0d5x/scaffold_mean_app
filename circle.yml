machine:
  node:
    version: latest
dependencies:
  pre:
    - npm prune
    - npm install -g npm@latest
    - npm install -g gulp-cli
    - gulp build
    - npm install --no-optional
test:
  override:
    - npm test
