machine:
  node:
    version: 6.7.0
dependencies:
  pre:
    - rm -rf node_modules
    - npm install -g npm@3.10.8
    - npm rebuild node-sass
    - npm install --no-optional
test:
  override:
    - npm test
