machine:
  node:
    version: 6.7.0
dependencies:
  pre:
    - npm prune
    - npm install -g npm@latest
    - npm install -g gulp-cli
    - npm rebuild node-sass
    - gulp build
    - npm install --no-optional
test:
  override:
    - npm test
